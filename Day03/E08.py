# Calculate the slope, x-intercept and y-intercept of y = 2x -2
# y=ax+b
# xIntercept, when y=0
#   ax+b=0, x=-b/a
# yIntercept, when x=0
# y=b

print("Consider the equation: y=ax+b")
a = float(input("Enter a: "))
b = float(input("Enter B: "))
xIntercept = -b/a
yIntercept = b
print(f"X Intercept = {xIntercept}")
print(f"Y Intercept = {yIntercept}")